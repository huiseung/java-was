package codesquad.adapter;

import codesquad.handler.Handler;
import codesquad.http.HttpRequest;
import codesquad.http.HttpResponse;

public class OperationHandlerAdapter implements HandlerAdapter{
    @Override
    public void handle(HttpRequest request, HttpResponse response, Handler handler) {

    }
}
